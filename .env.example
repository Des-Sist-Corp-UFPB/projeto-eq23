# =============================================================================
# Variáveis de ambiente — EXEMPLO
# =============================================================================
# INSTRUÇÕES:
#   1. Copie este arquivo: cp .env.example .env
#   2. Preencha os valores reais no .env
#   3. NUNCA faça commit do .env (ele está no .gitignore)
#
# No servidor de produção, este arquivo fica em:
#   /home/ghactions/eq23/.env
# A variável APP_IMAGE é atualizada automaticamente pelo GitHub Actions.
# =============================================================================

# Imagem Docker da aplicação (atualizada automaticamente pelo CI/CD a cada deploy)
# Substitua "usuario" pelo seu usuário ou organização no GitHub.
APP_IMAGE=ghcr.io/usuario/mercado:latest

# Banco de dados
DATABASE_URL=jdbc:postgresql://postgres:5432/eq23
DATABASE_USERNAME=eq23
DATABASE_PASSWORD=sJX6LspwEeFN0LIPe8U2

# (Opcional) Porta da aplicação — padrão: 8123
# SERVER_PORT=8123

# Umami Analytics (Opcional)
# Exemplo 1 (Umami Cloud): UMAMI_SCRIPT_URL=https://cloud.umami.is/script.js
# Exemplo 2 (Self-Hosted VPS): UMAMI_SCRIPT_URL=https://eq23.dsc.rodrigor.com:3000/script.js
UMAMI_SCRIPT_URL=https://cloud.umami.is/script.js
UMAMI_WEBSITE_ID=


